// types
import { AuthProps } from "@/types/auth";

// ==============================|| CHECK MAIL LAYOUT ||============================== //

export default function Layout({ children }: AuthProps) {
  return <>{children}</>;
}
